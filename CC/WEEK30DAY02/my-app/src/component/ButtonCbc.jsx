import React, { Component } from 'react'

export class ButtonCbc extends Component {
    render() {
        return (
            <div>
                <button>{this.props.title }</button>
            </div>
        )
    }
}

export default ButtonCbc
